---
const navItems = [
	{ href: '/', label: 'Inicio' },
	{ href: '/cocinas', label: 'Cocinas' },
	{ href: '/experiencias', label: 'Experiencias' },
	{ href: '/nosotros', label: 'Nosotros' },
	{ href: '/contacto', label: 'Contacto' },
];
---

<header class="fixed top-0 left-0 right-0 z-50 bg-white/90 backdrop-blur-md border-b border-stone-200">
	<div style="max-width: 1280px; margin: 0 auto; padding: 0 1rem;">
		<nav>
			<div class="flex items-center justify-between h-16">
				<a href="/" class="text-xl font-bold tracking-tight" style="font-family: var(--font-heading); color: var(--color-primary);">
					mas<span style="color: var(--color-cta);">moebel</span>
				</a>
				<div class="hidden md:flex items-center justify-center gap-8 flex-1">
					{navItems.map((item) => (
						<a href={item.href} class="text-sm font-medium transition-colors duration-200" style="color: var(--color-text);" onmouseover="this.style.color='var(--color-cta)'" onmouseout="this.style.color='var(--color-text)'">{item.label}</a>
					))}
				</div>
				<div class="hidden md:block w-20"></div>
				<button id="mobile-menu-btn" class="md:hidden p-2" style="color: var(--color-text);" aria-label="Menú">
					<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
						<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
					</svg>
				</button>
			</div>
			<div id="mobile-menu" class="hidden md:hidden pb-4">
				{navItems.map((item) => (
					<a href={item.href} class="block py-2 text-sm transition-colors duration-200" style="color: var(--color-text);" onmouseover="this.style.color='var(--color-cta)'" onmouseout="this.style.color='var(--color-text)'">{item.label}</a>
				))}
			</div>
		</nav>
	</div>
</header>

<script>
	const btn = document.getElementById('mobile-menu-btn');
	const menu = document.getElementById('mobile-menu');
	btn?.addEventListener('click', () => menu?.classList.toggle('hidden'));
</script>
